# Mobile App (React Native)

> **Status**: Placeholder — planned for Phase 4

This package will contain the React Native companion app for iOS and Android.

## Planned Features

- Live stroke canvas (uses same `StrokeEvent` type from `@aipen/types`)
- Camera capture for handwriting photos
- BLE connection management for smart pens
- Note sync with user account

## Getting Started (when ready)

```bash
cd apps/mobile
pnpm install
npx expo start
```
