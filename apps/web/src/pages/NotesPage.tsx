// TODO: Implement NotesPage
// - Paginated list of user's notes
// - Search input (debounced, calls GET /api/notes?q=)
// - Thumbnail + title + date for each note
export default function NotesPage() {
  return <div className="p-8"><h1 className="text-xl font-semibold">My Notes</h1></div>;
}
